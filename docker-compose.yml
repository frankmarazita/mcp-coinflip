services:
  app:
    image: ${IMAGE_URL}
    container_name: ${PACKAGE_NAME}
    ports:
      - "8755:3000"
    environment:
      ENVIRONMENT: ${ENVIRONMENT}
      SENTRY_DSN: ${SENTRY_DSN}
    restart: on-failure:5
